# FixRAF — Task Closed (yield handshake bug)

## Result on disk
runtime.rs rAF/cAF rewritten at lines 2586-2626:
- requestAnimationFrame: clones job_queue, reads callback from args[0], checks is_callable, fires after 16ms via schedule_timer, passes DOMHighResTimeStamp (SystemTime since UNIX_EPOCH, ms) as callback arg, returns handle ID as f64.
- cancelAnimationFrame: clones job_queue, calls cancel_timer(id), returns undefined.
- Both wrap `unsafe { NativeFunction::from_closure(...) }` matching setTimeout/clearTimeout pattern at 1066-1087 and 1115-1123.

## Verification
cargo check -p oxibrowser-core → clean (only 2 pre-existing warnings unrelated to rAF).

## Known blocker
yield tool's `result` parameter is being stripped to `{}` on the wire, preventing terminal completion handshake. Bug reported via report_tool_issue twice and via /tmp/fixraf-completion.md.
